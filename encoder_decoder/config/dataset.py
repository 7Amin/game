# The facade training set consist of 400 images
BUFFER_SIZE = 180
# The batch size of 1 produced better results for the U-Net in the original pix 2 pix experiment
BATCH_SIZE = 1
# Each image is 256x256 in size
IMG_WIDTH = 256
IMG_HEIGHT = 256

PATH = "./../Pacman_Complete/data/{}/images/"
